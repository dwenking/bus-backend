spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/?readPreference=primary&appname=MongoDB%20Compass&directConnection=true&ssl=false  # 开启了用户验证
      database: bussystem

  neo4j:
    authentication:
      password: 20010605
      username: neo4j
    uri: bolt://localhost:7687

server:
  port: 8080